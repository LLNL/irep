This is documentation generated from the ``wkt_mesh.h`` described in
IREP's ``README.md``. ``wkt_mesh.h`` looks like this:

.. WKT headers don't lex as proper C, but prolog works ok :)
.. literalinclude:: /wkt/wkt_mesh.h
   :language: prolog

The fields in this table and its ``box`` subtable are documented below.
